import { RecipeWineCategoryPipe } from './recipe-wine-category.pipe';

describe('RecipeWineCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeWineCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
