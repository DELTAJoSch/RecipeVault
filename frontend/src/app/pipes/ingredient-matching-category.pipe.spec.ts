import { IngredientMatchingCategoryPipe } from './ingredient-matching-category.pipe';

describe('IngredientMatchingCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientMatchingCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
