import { WineCategoryPipe } from './wine-category.pipe';

describe('WineCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new WineCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
