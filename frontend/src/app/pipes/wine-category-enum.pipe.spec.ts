import { WineCategoryEnumPipe } from './wine-category-enum.pipe';

describe('WineCategoryEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new WineCategoryEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
