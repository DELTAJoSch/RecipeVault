import { RecipeWineCategoryLinkingPipe } from './recipe-wine-category-linking.pipe';

describe('RecipeWineCategoryLinkingPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeWineCategoryLinkingPipe();
    expect(pipe).toBeTruthy();
  });
});
