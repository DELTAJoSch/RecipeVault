import { WineCategoryInfoPipePipe } from './wine-category-info-pipe.pipe';

describe('WineCategoryInfoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WineCategoryInfoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
