import { IngredientCategoryExamplePipe } from './ingredient-category-example.pipe';

describe('IngredientCategoryExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientCategoryExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
